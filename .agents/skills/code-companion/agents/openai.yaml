interface:
  display_name: "Code Companion"
  short_description: "Repo-specific patterns and workflows for code-companion"
  default_prompt: "Use the code-companion repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true